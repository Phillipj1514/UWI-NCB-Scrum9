import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'scrum9-project',
  webDir: 'build',
  bundledWebRuntime: false
};

export default config;
